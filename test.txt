DATAA		SEGMENT
		STRING	DB		'Str_asm'
		VD		DD		78536FDAH
		VW		DW		4C34H
		VWA		DW		2BF2H

DATAA 		ENDS

DATAB		SEGMENT		
		DA		DW		678CH
		DC		DW		5432H
		ADRESS  DD 		FPROC
		ADR	    DD 		PROCA
DATAB ENDS
CODE	SEGMENT	

ASSUME CS:CODE, DS:DATAA, ES:DATAB

PROCESS		PROC
		MOV AX, VW[BP + SI]
		POP	VWA[BP+SI]
		RET
PROCESS		ENDP

PROCA		PROC
		MOV CX, OFFSET DA
		JNE PROCESS	
		RET
PROCA		ENDP
START	PROC
	JNE		LP
	PUSH DA
	PUSH DC
	CALL	PROCESS
	MOV BX, OFFSET PROCESS
	PUSH	OFFSET VD
	CALL BX
	LP:
	CALL ADRESS
START	ENDP
CODE	ENDS
CODEB SEGMENT
ASSUME CS:CODEB, ES:DATAA, DS:DATAB

FPROC PROC  
		MOV CX, OFFSET STRING
		CALL ADR
		CALL VD[BX + DI]
FPROC ENDP

CODEB ENDS
END	START



